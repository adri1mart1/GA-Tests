name: 'install requirements'
description: 'install requirements for GA tests repo'
inputs:
  who-to-greet:
    description: 'Who to greet'
    required: true
    default: 'unknown'
runs:
  using: 'composite'
  steps:
    - run: echo "install requirements for ${{ inputs.who-to-greet }}"
      shell: bash
